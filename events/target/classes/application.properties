## -- Name:
spring.application.name=events

## -- Data base:
spring.datasource.url = jdbc:mysql://localhost:3306/eventDB
spring.datasource.username = root
spring.datasource.password = Rlwl2023.
spring.datasource.driver-class-name = com.mysql.cj.jdbc.Driver

## -- JPA y Hibernate (Dialect):
## -- update: Ajusta la base de datos para que coincida con los cambios en el c�digo (sin borrar datos).
## -- create: Borra y vuelve a crear la base de datos desde cero (pierde datos).
## -- create-drop: Crea la base de datos al iniciar y la borra al cerrar la aplicaci�n.
## -- none: No realiza ning�n cambio en la base de datos autom�ticamente.

spring.jpa.hibernate.ddl-auto = update
spring.jpa.hibernate.dialect = org.hibernate.dialect.MySQLDialect
spring.jpa.show-sql = true
spring.jpa.properties.hibernate.format_sql = true